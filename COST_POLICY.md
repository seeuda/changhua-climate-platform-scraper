# 成本守則（給所有 AI 助手與協作者）

> 本檔案是硬性規範。任何 AI 助手（Claude、Gemini、Antigravity 等）在此專案
> 及後續氣候文件查詢工作中，必須遵守以下規則。使用者已多次因助手違反
> 這些原則而產生不必要費用（見 W04 專案的除錯紀錄）。

## 規則一：檢索一律走 Vertex AI Search，不准擅自改用 Gemini API

- 查詢知識庫的**唯一預設路徑**是 `query_search.py`（Discovery Engine
  Search API + 內建摘要），消耗的是 Vertex AI Search 的免費扣抵額。
- **禁止**「因為搜尋結果不理想，就抓原始文件用 Gemini API 自行分析」
  的 fallback 行為。搜尋結果不理想時，正確做法是：
  1. 調整查詢字串或 filter（structData 有 organization/report_type/
     publish_date 可篩）
  2. 檢查資料是否確實已索引（LRO 空殼陷阱）
  3. 向使用者回報並詢問，而不是默默改走計費 API

## 規則二：Gemini API 只在使用者明示同意時使用

允許的情境（仍需事先告知預估呼叫次數）：
- 使用者明確要求的批次分析任務（如 W04 的落差分析）
- Search 摘要功能無法達成的長文生成

執行批次 LLM 任務前必須：
1. 報告：筆數 × 每筆呼叫數 = 總呼叫數
2. **先跑 3-5 筆試批**，讓使用者確認輸出品質後才跑全量
3. 臨時性、小量的呼叫優先走 AI Studio 免費額度，不走 Vertex 計費端點

## 規則三：匯入資料前先在本機驗證，嚴禁「上傳後除錯」

W04 專案曾因資料問題將 1,446 份文件**重複匯入五次**，Layout Parser
解析費重複支付五次。因此：
- 上傳前必須在本機完成：編碼檢查（UTF-8）、控制字元清除、
  超長段落切斷（`consolidate.py` 已內建）
- 首次匯入一律用小批量（`--limit 5`）驗證流程與檢索品質
- 匯入後檢查**內容**而非僅檢查筆數（空殼陷阱：筆數增加但文件為空）
- LRO 執行 15 分鐘以上屬正常，客戶端逾時**不等於失敗**，
  嚴禁因逾時就重新匯入
- **不可信任副檔名與檔名**（2026-07 氣候平臺教訓）：伺服器檔名可能是
  百分比編碼且被截斷，副檔名會遺失導致真 PDF 被誤排除。檔案格式判定
  一律以魔術位元組為準（%PDF、PK zip 容器內容），不以檔名為準

## 規則四：版本選擇與預算防線

- 純文字語料（如本專案的彙整 txt）用 **Standard Edition** 即可；
  Enterprise 的 Layout Parser／表格註解只對 PDF/DOCX 有意義
- **預算警報必須追蹤「未扣抵前的原始用量」**：本帳戶的通用扣抵額是
  不定期人工領取，若警報計入扣抵額，淨支出趨近零、警報永遠不會響。
  建立預算時取消勾選「將折扣與扣抵額計入成本」，範圍限定
  Discovery Engine + Vertex AI，門檻設為單次領取額度的 50%／90%
- 助手在涉及計費操作的工作階段結束時，提醒使用者核對
  Billing 報表的 Discovery Engine 與 Vertex AI 原始用量，
  以及**扣抵額剩餘餘額**（Billing → 扣抵額）

## 規則五：Gemini Enterprise「應用程式」層 vs「資料儲存庫」層要分開看

2026-07 確認：Gemini Enterprise 實際上是兩本不同的帳，助手與使用者都
容易搞混：

| 層級 | 對應操作 | 計費/授權方式 |
|------|---------|--------------|
| **應用程式**（聊天介面、Agent，如「彰化淨零推廣計畫」） | 建 App 時 | **按席次訂閱**，30 天免費試用；到期未轉付費，官方文件寫明依標準保留政策刪除 |
| **資料儲存庫**（Discovery Engine 索引） | 建資料儲存庫時 | **標準 GCP 用量計費**（走 Cloud Billing 帳戶），跟上面的 App 訂閱**無關**，不受 App 試用期到期影響 |

**決策**：在還沒有具體的多使用者推廣計畫前，**不需要為了保留聊天介面
而訂閱付費 App**。App 層試用到期就讓它到期，不續訂：
- 資料儲存庫本體會保留（走既有 Cloud Billing，不因 App 訂閱到期被刪）
- 個人查詢需求改用 `query_search.py`（直接打 Discovery Engine Search
  API，不經過 App 層，也不用處理多使用者權限）
- 本機 `downloads\archive\`（原始檔）與 `downloads\consolidated\`
  （彙整檔＋manifest.jsonl）持續保留作為底稿；日後若有正式推廣計畫、
  需要多人使用的網頁聊天介面，直接編列 App 訂閱費用、重新匯入
  manifest.jsonl（索引若仍在則為更新，若已被清空則為全新匯入，兩種
  情況都只需幾分鐘，不需重跑爬蟲）
- 助手不應在使用者尚未提出具體推廣計畫時，主動建議訂閱 App 層

## 違規的代價（歷史教訓，非假設）

- 1,446 份 × 5 次重複匯入的解析費
- 115 案批次分析 V3→V7 五個版本的重跑 token 費
- 除錯期間累積的 Enterprise 檢索查詢費

這些都發生在 2026 年 6-7 月的 W04 專案，紀錄存於
`G:\我的雲端硬碟\Gemini Gems\docs\`。不要重演。
